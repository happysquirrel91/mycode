<%= form_for(@webinfo) do |f| %>
  <% if @webinfo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@webinfo.errors.count, "error") %> prohibited this webinfo from being saved:</h2>

      <ul>
      <% @webinfo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_field :url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
