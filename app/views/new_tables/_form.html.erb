<%= form_for(@new_table) do |f| %>
  <% if @new_table.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@new_table.errors.count, "error") %> prohibited this new_table from being saved:</h2>

      <ul>
      <% @new_table.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
